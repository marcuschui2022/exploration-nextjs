import {Metadata} from "next";

export const metadata: Metadata = {
    title: "Guest area",
};

export default function Page() {
    return (
        <div>
            <h1>Account page</h1>
        </div>
    );
}